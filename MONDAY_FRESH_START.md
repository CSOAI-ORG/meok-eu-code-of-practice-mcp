# ☀️ MEOK — Monday Fresh Start (after 2026-06-07)
*One page. What's live, what needs YOU, what's queued. Full detail in `_TABS/`.*

## ✅ DONE + LIVE (shipped this session, verified)
**meok.ai (Vercel):**
- GEO authority: `/constellation` hub, 4 `/best-ai-for-*` pages, `/developers` SDK hub, sitewide Person(Nick) schema — all 200, JSON-LD validated.
- `/developers` = the Dev-Platform product story (Python/TS/Go SDK quickstarts + CLI, real verified code).

**MEOK ONE (the VM, `meok-backend`):**
- Full responsive pass (os/dome/law mobile) · **Voice Mode** (reactive orb + live STT + TTS + hands-free loop) · **LAW physical-safety crosswalk** (28 frameworks — robots-in-homes: ISO 13482/10218/45001 + EU/UK machinery) · **characters reframe** (9 adult/OTT personas softened → wholesome, faith trio opt-in, **Anime Mode** + anime voice, collectible cards). All health-gated, funnel intact.
- Mac↔VM divergence RECONCILED (deploy from Mac safe again).

**GitHub hygiene:**
- 🔴→✅ **7 severed-brand repos (CSGA/TerraNova) were PUBLIC → all now PRIVATE.**
- Backups: `csoai-research-institute` pushed private; `councilof-ai` confirmed backed up.
- True inventory: **469 repos** (~400 MCP fleet) + found the Dev-Platform ecosystem + `lib2b`.

**SDK:** `meok-sdk` Python fixed to support **3.9+** (was 3.10-only crash), v0.1.1, pushed.

**Infra:** attestation-api back up (was down); Vercel **Pro** now (deploy cap gone).

## ⏳ NEEDS YOU (exact 30-second actions, by priority)
1. **DNS — make MEOK ONE public.** Namecheap → meok.ai → Advanced DNS → add `A` record: host `one`, value `35.242.143.249`. (VM + Caddy ready; this is the only thing between you and a public MEOK ONE.)
2. **Team-scoped Vercel token** → I delete ~20–25 duplicate/dead projects (see `_TABS/VERCEL_AUDIT.md`: 5 optimobiles, 4 templemans, leftover `dist`/`client`/`frontend`, etc.). Vercel → Settings → Tokens → Create → Scope: **`niks-projects` team**.
3. **Rotate the personal Vercel token** you pasted earlier (it's in the transcript; it didn't work anyway — wrong scope).

## 🔵 QUEUED (next, uncontested — pick one Monday)
- **A2A/ACP Agent Cards** for the MCP fleet via `lib2b` — the first-mover "on every protocol's map" play (nobody else does this). *Recommended.*
- **MCP cross-post sweep** — list all 271 on every directory (free; mcp-submit + openMCP cross-post). Directories are free — don't pay for "pro" placement.
- **Characters:** cute avatar art (needs an artist/gen), anime persona variants.
- **Chat UX:** real token streaming + markdown rendering.
- **PyPI:** republish meok-sdk v0.1.1 so 3.9 users get the fix.

## 🗂️ The system (so Monday starts clean)
- **`_TABS/`** = the coordination hub: `MEOK_ECOSYSTEM_TABS.md` (6-ecosystem inventory + tab starter), `STATUS.md` (live board), `INBOX.md` (cross-tab notes), `RECONCILIATION_2026-06-07.md`, `VERCEL_AUDIT.md`.
- **Rule that matters:** 1 tab per CODEBASE (not per concept). Multiple tabs on one repo = the divergence/clobber/deploy-cap pain we hit 3× today. Serialise shared resources (one repo, one Vercel project, one deploy quota).
- Branch `claude/meok-one` (clawd-workspace) holds the MEOK ONE work; `meok-ai` main holds meok.ai.
