{
  "name": "wowmcp-ai",
  "version": "1.0.0",
  "description": "wowmcp.ai — WoW MCP server for AI agents. Domain landing + download hub.",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'static site — no build step'",
    "deploy": "vercel deploy --prod"
  }
}
