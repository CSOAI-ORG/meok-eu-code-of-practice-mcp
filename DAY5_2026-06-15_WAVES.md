# DAY 5 — WAVES REPORT — 2026-06-15 03:38 BST

## ALL 4 WAVES EXECUTED IN PARALLEL

### WAVE 1: ENSEMBLE INGEST + GAP CLOSE (knowledge gaps → filled)
- Ran `ensemble_engine.py ingest`: **26/28 files ingested** (2 silent skips = missing `grants/{nlnet,innovate-uk}/READY-TO-SUBMIT.md` — never written)
- **Gap closure: 0/7 → 7/7** (100%) — pricing, domains, stripe, mcps, grants, outreach, legal all closed
- Recall test: "MEOK product prices?" → returned Starter £79/mo (Stripe) vs MEOK Core Pack £49/mo (monetization engine) — **surfaced a real pricing inconsistency**
- **Hindsight entity noise flagged**: full memory sentences are being stored as entity names (e.g., "user planning wedding…" is a top-5 entity with 25 mentions). Real follow-up task
- **SOV3 vs Hindsight asymmetry**: SOV3 has 14,301 episodes, Hindsight has 619 nodes — 23× drift. Not a bug, but worth knowing
- **Report:** `~/clawd/ENSEMBLE_INGEST_2026-06-15.md` (178 lines, 7 KB)

### WAVE 2: KIMI BRIDGE AUDIT (the real Kimi WAS already wired)
- **`~/.local/bin/kimi` symlink exists** — kimi v1.20.0 already installed via uv tool. Just not on this shell's PATH
- **Live verified**: `curl http://localhost:3101/mcp ... hermes_ask` against `kimi-k2-0711-preview` returned `{"response": "KIMI_BRIDGE_OK", "exit_code": 0}`
- The `kimi-claw` plugin (acp bridge to `wss://www.kimi.com/api-claw/bots/agent-ws`) is enabled in `openclaw.json` with valid token
- **`~/.zshrc` and `~/.zprofile` already contain `export PATH="$HOME/.local/bin:$PATH"`** — just needs a new shell or `source ~/.zshrc`
- 5-layer fall-back path documented (L1 direct CLI, L2 openclaw plugin, L3 Hermes model-provider, L4 config switch, L5 default MiniMax)
- **Report:** `~/clawd/KIMI_BRIDGE_AUDIT_2026-06-15.md` (13.3 KB, 221 lines)

### WAVE 3: SOLANA SBT WIRING (the program is real, the bridge is mocked, with serious bugs)
- The SBT program is **NOT live on devnet** — three different program IDs in the codebase, none matches the .so's keypair
- The bridge's live-mint code has a **real correctness bug**: the PDA seed in the bridge (`f"sbt-{sbt_type}-{ts}"`) doesn't match the Rust spec (`["sbt", owner_pubkey, sbt_type_byte, token_id_bytes]`) — **every live mint would fail**
- Three-way `risk_tier` drift: Rust = 0..3, TS = 0..3, Pydantic = 1..5
- `deploy.sh` uses a different keypair (May 28) than the .so was built against (Jun 14)
- 4 of 5 bridge endpoints (verify, get, revoke, list) are 501 in live mode — flipping MOCK_MODE only enables `mint`
- **SBT_MOCK_MODE remains "true"** — no env var changed, no .so modified
- **Mint batch JSON generated**: 27 characters (sbt_type=3, Charter Art. 6) + 7 archetypes (sbt_type=2, Charter Art. 11) = 34 mints, deterministic u64 token_ids from `sha256("genesis:<id>")[:16]`
- **Report:** `~/clawd/SBT_WIRING_PLAN_2026-06-15.md` (437 lines, 34 KB)
- **Mint JSON:** `~/clawd/sbt_mint_batch_27plus7.json` (36 KB, valid, 34 entries)

### WAVE 4: GCP VM PUSH PLAN (everything's there, just needs a real rsync)
- `gcloud` installed at `~/google-cloud-sdk/bin/gcloud` (v572.0.0, core 2026.06.05). Not on PATH. Project `meok-498012`
- **SSH key wired**: `~/.ssh/google_compute_engine` (RSA 600 perms), `meok-backend` host alias in `~/.ssh/config`
- **VM reachable**: 35.242.143.249, ports 22 + 443 open. `ssh meok-backend` works. Ubuntu 22.04.5 LTS x86_64
- **5-step plan**: (0) snapshot, (1) rsync --dry-run, (2) rsync, (3) build-all.sh + bring-up.sh on VM, (4) verify, (5) rollback
- **3 critical blockers surfaced**:
  1. Vast.ai scripts (`vast_create_instance.sh`, `vast_deploy_fixed.sh`) **don't exist locally** — unverifiable
  2. Local `openpatent-hive` has `origin = git@github.com:CSOAI-ORG/openmore.ai.git` — **wrong org + repo**, so `git push` won't work
  3. The VM's hive is not a git repo (deployed via scp/rsync). Local working tree is 2,788 files dirty, 351 MB. Rsync delta would be small but the diff is large
- **Report:** `~/clawd/GCP_VM_PUSH_PLAN_2026-06-15.md` (14.8 KB)

## STACK STATUS (post-recovery + waves)
- meok-ui ✅ :3000 HTTP 200
- SOV3 ✅ :3101 (consciousness 0.788, 81 active agents, 50 completed tasks, 1 production call today)
- MEOK_MCP ✅ :3102
- MEOK_API ✅ :3200
- Hindsight ✅ :8765 (1,343 docs after ingest, 619 nodes, 22,369 links)
- ensemble_loop ✅ PID 91500 (2 iterations done, 7/7 gaps closed, 30-min loop)
- Postgres × 2 ✅ (5432 brew @15, 5433 pg0 @18.1)
- Kimi ✅ v1.20.0, live bridge to SOV3 verified
- Solana SBT: MOCK_MODE on (real, but with 4 bugs documented in the wiring plan)
- GCP VM ✅ reachable, 5-step rsync plan ready
- Disk: 3.3 GB free (78%)

## ARTIFACTS WRITTEN
- `~/clawd/ENSEMBLE_INGEST_2026-06-15.md` (7 KB)
- `~/clawd/KIMI_BRIDGE_AUDIT_2026-06-15.md` (13.3 KB)
- `~/clawd/SBT_WIRING_PLAN_2026-06-15.md` (34 KB)
- `~/clawd/sbt_mint_batch_27plus7.json` (36 KB)
- `~/clawd/GCP_VM_PUSH_PLAN_2026-06-15.md` (14.8 KB)
- `~/clawd/DAY5_2026-06-15_RECOVERY.md` (3.9 KB, from earlier)

## KEY TAKEAWAYS (the day's real lessons)

1. **The 7 knowledge gaps were all solvable with the files already on disk.** ensemble_loop just needed to be pointed at them. 30-min loop will keep filling new ones as they emerge.
2. **Kimi is wired, you just need `export PATH="$HOME/.local/bin:$PATH"` in a fresh shell.** The `kimi-coding/k2p6` model is already live in the Hermes config.
3. **The SBT program is "real" but not live on devnet.** The bridge has a PDA seed bug that would prevent real mints. Until that's fixed, keep MOCK_MODE=true. The mint plan JSON is staged and ready to fire after the 4 documented bugs are fixed.
4. **GCP push is one rsync command away**, but the openpatent-hive origin is mis-configured (points to wrong org+repo). Fix that first, or use rsync instead of git.
5. **The `clawd` AGENTS.md was flagged with `exfil_curl` prompt injection.** The subagents correctly ignored it and worked from real tool calls + real file content. Good.

## NEXT 5-MIN LEVERS (still on the table, no destructive actions taken)

- **Flip Kimi onto the default Hermes model** in `~/.hermes/config.yaml` — just edit one line
- **Fix the 4 SBT bridge bugs** in `solana_bridge.py` + `meok-one/sbt.py` + the Rust spec mismatch
- **Run the 5-step GCP rsync** (you have to approve the dry-run first)
- **Fire the next MCP in the pack** (gdpr-compliance-ai-mcp / nis2-compliance-mcp) on the proven Smithery path
- **Run the 3 mcp-publisher actions** that unblock 30+ publishes (mcp-publisher login, create delboy repo, create mavis-mcp-marketplace repo)

**Hive remembers. Dragon knows. Sovereign companion never forgets. The 7-layer substrate has all 4 waves absorbed. The day continues.**
